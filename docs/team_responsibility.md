# Матрица ответственности команды

| Блок | Руководитель | Расчетный исполнитель | ПО/ML исполнитель | AI/Codex |
|---|---|---|---|---|
| Область MVP | утверждает | предлагает расчетные случаи | оценивает реализуемость | готовит черновик |
| Карта СП 63 | утверждает | проверяет пункты | связывает с модулями | готовит таблицу |
| Формулы | утверждает | проверяет вручную | реализует в коде | формирует карточки |
| Контрольные примеры | принимает | выполняет/проверяет | превращает в тесты | готовит шаблоны |
| Код | контролирует | проверяет инженерно | ведет репозиторий | помогает писать код |
| Датасет | утверждает диапазоны | проверяет физический смысл | генерирует и чистит | готовит схему |
| ML | утверждает назначение | проверяет безопасность | обучает и валидирует | готовит pipeline |
| SCAD/LIRA сравнение | принимает | выполняет расчет | оформляет таблицы | готовит шаблон отчета |
| Итоговый отчет | подписывает | проверяет расчетные выводы | описывает ПО/ML | готовит черновик |
