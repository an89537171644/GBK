# Шаг 3. Самопроверка устранения расхождений ULS-BEND-RECT-001

## 1. Предмет и вывод

Самопроверка предназначена для контроля полноты пакета шага 3 относительно
замечаний шага 2. Она не является независимым инженерным расчётом.

Дата среза: 2026-07-15. Интеграционная перепроверка: 2026-07-16.

Текущая диспозиция: `RETURN_FOR_ENGINEERING_REVIEW`.

- `requires_engineer_review=true`;
- `project_use=false`;
- нормативные формулы настоящей самопроверкой не утверждаются;
- синтетические и самосогласованные тесты не считаются независимым evidence.

## 2. Проверенная исходная граница

| Свойство | Результат | Статус | Инженерная проверка |
|---|---|---|---|
| исходный baseline разработки | `K108@086cd1c42dd8098b703e3880c2d9101f5ccf9635` | `CONFIRMED` | не требуется для SHA; требуется для содержания |
| интеграционный baseline перед PR | K109, `main@d3b4e40514b369eccee6c4be66c3863528b15291` | `CONFIRMED` по SHA; содержание K109 не является нормативным evidence шага 3 | требуется для совместимости |
| рабочий процесс | отдельная ветвь `codex/128-uls-bend-rect-step3`, Issue № 128 | `CONFIRMED` | нет |
| нормативный профиль | программный ID `SP63-2018-AMD1-AMD2-PROVISIONAL@2026-07-15`; source SHA-256 `8dfe7fc1af47d6adf2a4d91ed91ee92fe0762abe20a0c54b3c248c7ff138fe00` | source content `CONFIRMED`; официальный статус, актуальность после Изменения № 2 и sign-off `OPEN_QUESTION` | обязательна при выпуске |
| приложенный PDF | исходное издание, SHA-256 `075af4fd7758504d56b9b17b9e0e7eb6fe74e531236dfcabc2016d75436a9222` | `CONFIRMED` как файл; единственный актуальный источник `REJECTED` | обязательна |
| итоговый SHA результата | формируется после настоящего уточнения документации и фиксируется в draft PR | `OPEN_QUESTION` до формирования PR | обязательна |

## 3. Матрица закрытия замечаний шага 2

| ID шага 2 | Проверяемое закрытие | Статус решения | Критерий самопроверки | Инженерная проверка |
|---|---|---|---|---|
| `CR-01` | строки A400 и зависимые ожидаемые значения шага 2 | значения 390/340/280 `CONFIRMED` как source content; ожидаемые BMR `ASSUMPTION`; sign-off `OPEN_QUESTION` | BMR-02 должен перейти к `fail`; построчная сверка каталога по официальной актуальной редакции | обязательна |
| `CR-02` | `gamma_b1` применяется к `Rb`, а `Rsc` разрешается тем же контекстом | `CONFIRMED` для `short/long`; прочие `gamma_bi` — `OPEN_QUESTION` | BMR-03 должен перейти к `fail`; в протоколе видны base/coefficient/effective | обязательна |
| `CR-03` | при `x>xi_R*h0` способность не публикуется | `CONFIRMED` как безопасная граница | BMR-05: `outside_applicability`, `M_ult` и utilization отсутствуют | обязательна |
| `CR-04` | профиль и точные источники сохранены | `CONFIRMED` как контракт | результат не ограничивается строкой «8.1.8–8.1.9» | обязательна |
| `SG-01` | локальные оси, ось момента и растянутая грань обязательны | `ASSUMPTION` как машинная конвенция | отсутствующие/недопустимые данные отвергаются до расчёта | обязательна |
| `SG-02` | определена семантика `cover` | `ASSUMPTION` | документация и геометрический протокол используют одно определение | обязательна |
| `SG-03` | отсутствует произвольный `h0_override` в проектном пути | прежнее поведение `REJECTED` | проверка и подбор получают `h0` из одной геометрии | обязательна |
| `SG-04` | нет неявного диаметра сжатой арматуры | прежнее поведение `REJECTED` | версия 1 не строит ветвь сжатой арматуры | обязательна |
| `SG-05` | `As_prime>0` явно вне сквозной версии 1 | `ASSUMPTION` | BMR-04 возвращает `outside_applicability` | обязательна |
| `SG-06` | отсутствует произвольный `Rsc_override` | прежнее поведение `REJECTED` | значение приходит только из профиля/контекста | обязательна |
| `SG-07` | B15 `Rbtser` исправлен | `CONFIRMED` как требование | построчная сверка и зависимые тесты | обязательна |
| `SG-08` | непокрытый п. 8.1.3 не маскируется общим `pass` | `OPEN_QUESTION` по содержанию; обработка `CONFIRMED` | completeness остаётся `incomplete` | обязательна |
| `SG-09` | внутренние cases не объявлены независимыми | прежняя трактовка `REJECTED` | документация сохраняет `ASSUMPTION` и запрос подписи | обязательна |
| `SG-10` | синтетический sample не открывает проектный шлюз | прежняя трактовка `REJECTED` | `project_use=false` независимо от инфраструктурной метки | обязательна |
| `SG-11` | допуски не меняют расчётный статус | единая политика `OPEN_QUESTION` | сравнение `M` и `M_ult` выполняется по неокруглённым значениям | обязательна |

Общая CSV-фикстура проверки материалов является `synthetic/non-evidence`.
Строка B15 и пять проверенных строк A400
не наследуют прежнюю условную подпись: они имеют
`needs_review`, пустые поля инженера/даты и не закрывают material gate.

## 4. Контроль BMR

| Случай | Требуемый исход | Доказательная оценка | Статус до подписи инженера |
|---|---|---|---|
| `BMR-01` | `pass` узкого ULS-сравнения | арифметическая регрессия | `ASSUMPTION` |
| `BMR-02` | `fail` с source-confirmed A400 | регрессия шага 2; ожидаемое число не подтверждено независимо | `ASSUMPTION` |
| `BMR-03` | `fail` при `gamma_b1=0,9` | регрессия контекста нагрузок | `ASSUMPTION` |
| `BMR-04` | `outside_applicability` для `As_prime>0` | контроль границы версии 1 | `ASSUMPTION` |
| `BMR-05` | `outside_applicability`, без `M_ult` и utilization | контроль безопасной остановки | `ASSUMPTION` |

Даже успешное автоматическое воспроизведение всех строк таблицы подтверждает
поведение реализации, но не независимость ожидаемых значений.

## 5. Проверки репозитория перед передачей

Перед формированием PR должны быть выполнены и приложены к handover:

```text
python -m pytest
ruff check .
python -m sp63_core validate --golden
python -m sp63_core validate --generate-dataset-limit 20 --json
python -m sp63_core generate-dataset --limit 20 --load-duration short --split --group-split --output-dir data/generated --prefix smoke_dataset --report reports/interim/smoke_dataset_report.json
python -m sp63_core train-baseline --generate-dataset-limit 50 --model-output models/smoke_baseline.pkl --metrics-output reports/interim/smoke_baseline_metrics.json
```

| Проверка | Ожидаемый критерий | Статус в настоящем документе |
|---|---|---|
| полный набор `pytest` | обычные тесты проходят; `protected-files-check` обязан блокировать ветвь до инженерного решения по изменённым защищённым файлам | фиксируется отдельно для pre-commit и commit-aware запусков |
| `ruff` | без ошибок | результат фиксируется исполнителем после сведения общего diff |
| golden validation | 12 регрессий, включая BMR-01—BMR-05, проходят в обычном и acceptance-маршрутах | автоматический успех не равен инженерному evidence; BMR сохраняют отдельный provenance |
| ограниченная генерация dataset | обе dataset-команды завершены успешно; версия 0.3, `short` и проектные флаги проверены | synthetic output не используется как нормативное доказательство |
| ML smoke | обучение на 50 синтетических строк завершено; safety/readiness gates остаются закрытыми для project use | метрики не являются нормативным evidence |
| diff от интеграционного baseline | архитектуры моделей, определения признаков и обученные веса без изменений; dataset/readiness contracts, guards обучения/предсказания и safety wrappers адаптированы к обязательному provenance API | проверяется перед PR |
| `main` | K109 `d3b4e405...` не изменяется прямым push; работа ведётся только в Issue-ветви | проверяется перед PR |

Самопроверка документации не подставляет вымышленные результаты команд. Их
точный вывод и SHA должны быть получены на окончательном состоянии ветви.

### 5.1. Фактический прогон перед commit

| Команда | Фактический результат | Доказательная граница |
|---|---|---|
| `python -m pytest` | `854 passed in 37.34s` | подтверждает только автоматизированное поведение |
| `ruff check .` | `All checks passed` | статический контроль, не инженерная проверка |
| `python -m sp63_core validate --golden` | `12/12` regression matches; BMR-02/03 имеют фактический расчётный `fail`, BMR-04/05 — `outside_applicability` | BMR остаются `ASSUMPTION` |
| `python -m sp63_core validate --generate-dataset-limit 20 --json` | 20 строк, `unsafe_rows_count=0`, validation `pass` | `incomplete`, `needs_engineer_review`, `project_use=false` |
| обязательный `generate-dataset` smoke | 20 строк, schema `0.3`, `load_duration=short`, 0 unsafe/mismatch | синтетический набор, не external evidence |
| обязательный `train-baseline` smoke | 50 строк; обучение завершено, `ml_quality_status=warning`, `unsafe_prediction_rate=0.777778`, `deterministic_accept_rate=0.222222` | `ml_ready_for_project_use=false`; модель удалена после smoke |

Значения зафиксированы для pre-commit worktree 2026-07-15. Commit SHA и PR
фиксируются отдельно после создания неизменяемого commit; это не меняет
инженерные блокеры раздела 7 handover.

### 5.2. Commit-aware прогон после интеграции K109

После переноса ветви на `main@d3b4e40514b369eccee6c4be66c3863528b15291`
повторный прогон дал следующие результаты:

| Команда | Фактический результат | Вывод |
|---|---|---|
| полный `pytest` | `843 passed, 20 failed in 46.26s` | все 20 отказов каскадно вызваны штатным `protected-files-check` |
| `protected-files-check --json` | `fail`; обнаружены изменения `bending.py`, `cracking.py`, `shear.py`, `concrete.py`, `rebar.py`, `validation/external.py` | обязательная остановка до инженерного решения; исправлять тесты или снимать guard запрещено |
| `ruff check .` | `All checks passed` | статический контроль пройден |
| `git diff --check origin/main...HEAD` | без замечаний | формат diff корректен |
| `validate --golden` | `12/12`; расчётные исходы BMR совпали с regression expectations | `incomplete`, `needs_engineer_review`, `project_use=false` сохранены |
| `validate --generate-dataset-limit 20 --json` | 20 строк, 0 unsafe/mismatch/duplicate/leakage | синтетический контроль не является нормативным evidence |
| обязательный `generate-dataset` smoke | 20 строк, schema 0.3, `short`, 0 unsafe/mismatch | `project_use=false` |
| обязательный `train-baseline` smoke | 50 строк, `ml_quality_status=warning`, `unsafe_prediction_rate=0.777778`, `deterministic_accept_rate=0.222222` | `ml_ready_for_project_use=false` |
| рабочее дерево | чистое до настоящего уточнения документации | непредусмотренные артефакты отсутствовали |

Таким образом, ветвь пригодна только для чернового PR и инженерной проверки.
Статус защитного шлюза не трактуется как вычислительная регрессия, но он
запрещает слияние и проектное применение до независимого рассмотрения
изменённых нормативно чувствительных файлов.

## 6. Контроль обязательных категорий

| Требование | Где отражено | Результат |
|---|---|---|
| источник, статус, архитектурное влияние и инженерная проверка для решений | разделы 3–4 и 6–10 `step3_uls_bend_rect_remediation.md` | `CONFIRMED` по структуре |
| противоречия | раздел 6 remediation | `CONFIRMED` по структуре |
| отсутствующие условия применимости | раздел 7 remediation | `CONFIRMED` по структуре; содержание остаётся `OPEN_QUESTION` |
| формулы без точной ссылки | раздел 8 remediation и обновлённая формульная карточка | `CONFIRMED` по структуре |
| неоднозначные обозначения | раздел 9 remediation | `CONFIRMED` по структуре |
| неподтверждённые коэффициенты | раздел 10 remediation | `CONFIRMED` по структуре; допуски `OPEN_QUESTION` |
| остановка до утверждения формул | remediation, self-audit и handover | `CONFIRMED` |

## 7. Что самопроверка не закрывает

| Незакрытый предмет | Статус | Последствие |
|---|---|---|
| подпись инженера по нормативному профилю и каталогу | `OPEN_QUESTION` | `project_use=false` |
| независимый расчёт BMR другим исполнителем | `OPEN_QUESTION` | BMR остаются `ASSUMPTION` |
| реальное внешнее сопоставление | `OPEN_QUESTION` | evidence не готово к проектному допуску |
| утверждённая политика допусков и округления | `OPEN_QUESTION` | сравнения пригодны только как регрессии |
| условие п. 8.1.3 | `OPEN_QUESTION` | completeness остаётся `incomplete` |
| полный набор `gamma_bi` | `OPEN_QUESTION` | область контекста нагрузок остаётся узкой |
| формальная проверка предпосылок п. 8.1.4 | `OPEN_QUESTION` | входной scope требует ручного подтверждения |
| двойное/многорядное армирование и ветвь п. 8.1.12 | `OPEN_QUESTION` | `outside_applicability` |

## 8. Итоговая диспозиция

Пакет шага 3 может быть передан только как объект повторной инженерной
проверки. Разрешённый итог после успешных автоматизированных проверок —
`READY_FOR_ENGINEERING_REVIEW`, но не `APPROVED_FOR_PROJECT_USE`.

До подписанного решения инженера сохраняются:

```text
requires_engineer_review = true
evidence_status = needs_engineer_review
project_use = false
```
