# NOTATION

## 1. Статус документа

Пакет: шаг 1 миграции проекта «ЖБК программа СП 63».

Статус: `DRAFT_FOR_ENGINEERING_REVIEW`. Машинные ключи фиксируются как факт
текущей реализации; их тождество нормативным обозначениям не утверждается.

## 2. Правило обозначений

| Решение | Источник | Статус | Влияние на архитектуру | Инженерная проверка |
|---|---|---|---|---|
| хранить устойчивый ASCII-ключ отдельно от инженерного символа в отчёте | прежний `NOTATION.md`; структура репозитория | `ASSUMPTION` | позволяет исправлять отображение без скрытого изменения API | обязательна для инженерного символа |
| при первом появлении показывать название, символ и единицу | прежние миграционные документы | `ASSUMPTION` | единый словарь нужен входам, ядру и отчёту | обязательна |
| не использовать одно имя для разных физических величин без контекста модуля | аудит K108/K109 | `CONFIRMED` как требование безопасности | исключает неоднозначные протоколы | обязательна для перечня величин |

## 3. Основной словарь ULS-BEND-RECT-001

| Ключ | Рабочее прочтение | Единица | Источник | Статус | Архитектурное влияние | Инженерная проверка |
|---|---|---:|---|---|---|---|
| `b` | ширина прямоугольного сечения | мм | `sections/rectangular.py`; прежний пакет | `CONFIRMED` как поле | требует явной локальной системы осей | да |
| `h` | полная высота сечения | мм | тот же источник | `CONFIRMED` как поле | ориентация граней не выводится из имени | да |
| `h0` | рабочая высота до центра растянутой арматуры | мм | `sections/rectangular.py` | `CONFIRMED` как вычисляемое поле; инженерный смысл `ASSUMPTION` | должна происходить из трассируемой геометрии | обязательно |
| `cover` | расстояние от грани бетона до наружной поверхности хомута в узкой однорядной модели | мм | текущий `sections/rectangular.py`; шаг 3 | `CONFIRMED` как поведение; применимость `ASSUMPTION` | форма ввода должна показывать точку отсчёта | обязательно |
| `stirrup_diameter` | диаметр хомута, участвующий в геометрии | мм | `sections/rectangular.py` | `CONFIRMED` как поле | должен быть связан с принятой схемой армирования | обязательно |
| `main_bar_diameter` | диаметр растянутого продольного стержня | мм | `sections/rectangular.py` | `CONFIRMED` как поле | текущая геометрия предполагает один слой | обязательно |
| `As` | площадь растянутой продольной арматуры | мм² | `checks/bending.py` | `CONFIRMED` как вход; нормативная роль `ASSUMPTION` | схема стержней должна быть связана с площадью | обязательно |
| `As_prime` | площадь сжатой продольной арматуры | мм² | прежняя реализация и шаг 3 | `REJECTED` для версии 1 при ненулевом значении | отдельная спецификация для двустороннего армирования | обязательно |
| `M` | неотрицательный модуль расчётного изгибающего момента при отдельно заданной растянутой грани | Н·мм | `checks/bending.py`; `sections/orientation.py` | `CONFIRMED` как контракт кода; инженерная конвенция `ASSUMPTION` | знак не должен неявно выбирать грань | обязательно |
| `x` | высота сжатой зоны | мм | текущая реализация; карточка 8.1.9 | `ASSUMPTION` до утверждения формулы | промежуточное значение требует trace ID | обязательно |
| `xi` | относительная высота сжатой зоны | безразм. | тот же источник | `ASSUMPTION` | в отчёте нужен символ `ξ` и точное определение | обязательно |
| `xi_R` | предельная относительная высота сжатой зоны | безразм. | тот же источник | `ASSUMPTION` | является шлюзом области применимости | обязательно |
| `Mult` | машинный ключ несущей способности по моменту | Н·мм | `checks/bending.py` | `CONFIRMED` как ключ; не подтверждён как нормативный символ | не смешивать с входным `M` | обязательно |
| `utilization` | программное отношение эффекта к сопротивлению | безразм. | расчётное ядро | `CONFIRMED` как метрика | не означает проектный допуск | да |
| `Rb`, `Rbt`, `Rbser`, `Rbtser`, `Eb` | свойства бетона по назначению каталога | МПа | таблицы 6.7, 6.8, 6.11; паспорт источника | `CONFIRMED` как содержание проверенных строк; назначение в алгоритмах требует review | отдельный версионируемый каталог | обязательно |
| `Rsn`, `Rs`, `Rsser`, `Rsc`, `Rsw`, `Es` | свойства арматуры по назначению каталога | МПа | таблицы 6.13–6.15; текущий каталог | `CONFIRMED` для визуально сверенных строк; алгоритмическое применение `ASSUMPTION` | каталог не должен допускать произвольный override | обязательно |
| `gamma_b1` | коэффициент условий работы бетона, выбираемый из объявленного сочетания | безразм. | пункт 6.1.12, PDF 25 | `CONFIRMED` как содержание источника | нужен типизированный контекст нагрузок | обязательно |
| `load_duration` | машинная категория `short`/`long` | код шага 3 | `ASSUMPTION` как модель нормативного контекста | сквозной `long` пока запрещён | обязательно |

## 4. Неоднозначности

| ID | Неоднозначность | Источник | Статус | Влияние | Инженерная проверка |
|---|---|---|---|---|---|
| `N-AMB-01` | `Mult` — технический ключ, а не утверждённое нормативное обозначение | код | `OPEN_QUESTION` | влияет на API и отчёт | обязательна |
| `N-AMB-02` | `short`/`long` недостаточно сами по себе описывают сочетание воздействий | п. 6.1.12; код | `OPEN_QUESTION` | требуется явный объект расчётной ситуации | обязательна |
| `N-AMB-03` | прежние документы называли `cover` просто защитным слоем без точки отсчёта | прежний пакет и текущий код | прежняя формулировка `REJECTED` | миграция входных данных требует преобразования | обязательна |
| `N-AMB-04` | `status=pass` может читаться как нормативное одобрение | отчёты K108/K109 | `OPEN_QUESTION` | нужны отдельные calculation/evidence/project-use статусы | обязательна |
| `N-AMB-05` | `Rsc_short`/`Rsc_long` смешивают свойство материала и условие сочетания | каталог и п. 6.2.8 | `OPEN_QUESTION` | значение должно разрешаться контекстом, а не ручным выбором | обязательна |

## 5. Запрещённые обходы

| Решение | Источник | Статус | Влияние | Инженерная проверка |
|---|---|---|---|---|
| произвольный `h0_override` не входит в проектный путь | шаг 3 | прежний обход `REJECTED` | сохраняет происхождение геометрии | обязательна |
| произвольный `Rsc_override` не входит в проектный путь | шаг 3 | прежний обход `REJECTED` | свойства разрешаются из версии каталога и ситуации | обязательна |
| строка схемы армирования не является единственным источником геометрии | прежний пакет | `ASSUMPTION` | расчёт использует структурированные поля | обязательна |

Точка остановки: словарь передаётся инженеру; окончательные нормативные символы
и определения не утверждены.
